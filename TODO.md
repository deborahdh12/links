# TODO List for Cecil Project Modifications

- [x] Update `cecil.yml` to match the provided template:
  - Ensure baseline and description are set as per template.
  - Simplify social entries to only include url.
  - Uncomment and set manifest background_color to "#fff".
  - Change avatar to avatar.png if necessary.
- [x] Update `pages/index.md` to add sample content in the body after frontmatter.
- [ ] Run `php cecil.phar serve --open` to preview the site.
- [ ] Run `php cecil.phar build` to build the site.
- [ ] Test and verify the changes.
